name: left-ci
version: 0.1.0.0

dependencies:
  - base >= 4.7 && < 5
  # - either >= 5
  - text
  - containers

library:
  source-dirs: src
  exposed-modules:
    - Config.ApplicationConfig
    - Config.Validator
    - Server.BuildService
    - Server.Routes
    - Server.Domain
  dependencies:
    - directory
    - network
    - scotty
    - wai
    - http-types 
    - warp
    - aeson
    - bytestring


executables:
  left-ci:
    main: Main.hs
    source-dirs: app
    dependencies:
      - left-ci
      - warp



tests:
  unit:
    dependencies:
      - left-ci
      - HUnit
    main: Main.hs
    source-dirs: test/unit

  integration:
    dependencies:
      - left-ci
      - HUnit
      - directory
      - network
      - wai
      - bytestring
      - wai-extra
      - http-types
      - scotty
      - aeson
    main: Main.hs
    source-dirs: test/integration